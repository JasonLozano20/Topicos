<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:AgendaPersonal_MAUI.Views"
             x:Class="AgendaPersonal_MAUI.Views.ContactosPage"
             Title="Contactos">
    <VerticalStackLayout Padding="10">
        <Label Text="Mis Contactos" FontSize="Large" />

        <CollectionView x:Name="collectionView" ItemsSource="{Binding Contactos}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="5" Padding="10">
                        <StackLayout>
                            <Label Text="{Binding Nombre}" FontAttributes="Bold" />
                            <Label Text="{Binding Telefono}" />
                            <Label Text="{Binding Correo}" />
                            <Label Text="{Binding Direccion}" />

                            <VerticalStackLayout Spacing="10" VerticalOptions="End">
                                <Button  Text="Eliminar" BackgroundColor="Red"
                                      TextColor="White"
                                      Clicked="OnEliminarContacto"/>
                                
                                <Button Text="Editar" BackgroundColor="Yellow"
                                        TextColor="Black"
                                        Clicked="OnEditarContacto" />
                            </VerticalStackLayout>
                        </StackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>